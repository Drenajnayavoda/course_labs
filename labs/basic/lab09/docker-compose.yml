version: "3.9"
services:
  app:
    build: ./app
    image: lab09-app:latest
    container_name: lab09-app
    ports:
      - "8080:8080"
    environment:
      - FLASK_ENV=development
      - DEBUG=true
      - DB_PASSWORD=SuperSecret123
