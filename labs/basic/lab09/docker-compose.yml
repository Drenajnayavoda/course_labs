services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    image: lab09-app:latest
    container_name: lab09-app
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/tmp/app.db
    restart: unless-stopped
